function setPosition(ele: Element, direction: 'left' | 'top' | 'right' | 'bottom') {

}

let box11 = document.querySelector('div')

if(box11) {
    setPosition(box11, 'left')
    //setPosition(box11, 'a') // 报错
}
